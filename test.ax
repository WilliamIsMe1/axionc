#DEFINE MACRO ADD(x, y) x + y

import axion.math

string text = "Hello! This is a string! It uses quotes! Like this: \"\""
int num = 1 +1
num2 = ADD(1, num)
fraction confusing = 1 :3
bool to_be_or_not_to_Be = true
if (to_be_or_not_to_Be ) {
	// Testing comments and whitespace
    print(text, "other arg")
}

int hex_literal = 0x1023.CAp-10
/* Testing */
while (num2 == num1) {
   println("idk")
  break
}

"Just for fun, lets === put a triple"

sys.print(pwr(3,2)) // Print 3 squared

int testBoi(int param) {
    return param + 1
}

/*EOF */